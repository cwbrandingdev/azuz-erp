import { TenantNotFound } from "@/components/tenancy/tenant-not-found";

export const metadata = {
  title: "Workspace não encontrado",
};

export default function TenantNotFoundPage() {
  return <TenantNotFound />;
}
